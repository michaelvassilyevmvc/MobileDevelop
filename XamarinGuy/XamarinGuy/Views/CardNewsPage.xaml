<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:XamarinGuy.ViewModels"
             x:Class="XamarinGuy.Views.CardNewsPage"
             xmlns:view="clr-namespace:XamarinGuy.Views"
             xmlns:viewModel="clr-namespace:XamarinGuy.ViewModels"
             Title="Lets Hack It">
    <ContentPage.BindingContext>
        <viewModel:CardDataViewModel></viewModel:CardDataViewModel>
    </ContentPage.BindingContext>
    <StackLayout Orientation="Vertical">
        <Label Text="Google List View"
               VerticalOptions="Start"
               HorizontalTextAlignment="Center"
               VerticalTextAlignment="Center"
               BackgroundColor="Transparent"
               HorizontalOptions="CenterAndExpand"></Label>
        <ListView x:Name="listView" SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                  RowHeight="150" ItemsSource="{Binding CardDataCollection}" HasUnevenRows="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <view:CardViewTemplate></view:CardViewTemplate>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>